import 'package:flutter/material.dart';

class EmployeeDetailViewPanel extends StatelessWidget {
  const EmployeeDetailViewPanel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
