import 'package:flutter/material.dart';

const loginTFPadding = EdgeInsets.symmetric(
  vertical: 5,
  horizontal: 22,
);

const panelsMargin = EdgeInsets.all(25.0);
const defaultPadding16 = EdgeInsets.all(16.0);
